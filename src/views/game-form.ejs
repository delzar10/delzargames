<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!--Import Google Icon Font-->
        <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <!--Import materialize.css-->
        <link type="text/css" rel="stylesheet" href="styles.css"/>
        <!--Let browser know website is optimized for mobile-->

        <!-- jQuery library -->
        <script src="../vendor.js"></script>
        <script src="http://malsup.github.com/jquery.form.js"></script> 
            <script>
                $(document).ready(function () {
                    var options = {
                        beforeSubmit: showRequest,  // pre-submit callback
                        success: showResponse  // post-submit callback
                    };

                    // bind to the form's submit event
                    $('#game-form').submit(function () {
                        $(this).ajaxSubmit(options);
                        // always return false to prevent standard browser submit and page navigation
                        return false;
                    });
                });

                // pre-submit callback
                function showRequest(formData, jqForm, options) {
                    alert('Uploading is starting.');
                    return true;
                }

                // post-submit callback
                function showResponse(responseText, statusText, xhr, $form) {
                    alert('status: ' + statusText + '\n\nresponseText: \n' + responseText );
                }
        </script>
        <!-- Latest compiled JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
     </head>
  <body>

    <%- include header %>

            <div class="container ">
                <div class="row padding-10 ">
                    <%- include sidebar-admin %>
            <div class="col-sm-8">
            <div class="std">
            <div class="cms-page">
                <h2>Game Form</h2>
            <!-- form action="/save-game" action="post" -->
            <form id="game-form" enctype="multipart/form-data" action="save-game" method="post">
                <div class="form-group"> <!-- Name field -->
                    <label class="control-label " for="name">Nombre</label>
                    <input class="form-control" id="gameName" name="gameName" data-validation="alphanum" type="text" placeholder="Nombre"/>
                </div>
                <span class="help-block"></span>

                <div class="form-group"> <!-- Email field -->
                    <label class="control-label requiredField" for="gameConsole">Consola<span class="asteriskField">*</span></label>
                    <input class="form-control" id="gameConsole" name="gameConsole" data-validation="alphanum" type="text" placeholder="Consola"/>
                </div>
                <span class="help-block"></span>

                <div class="form-group"> <!-- Subject field -->
                    <label class="control-label " for="gamePrice">Price</label>
                    <input class="form-control" id="gamePrice" name="gamePrice" data-validation="number" data-validation="number" type="text" placeholder="Precio"/>
                </div>
                <span class="help-block"></span>

                <div class="form-group"> <!-- Subject field -->
                    <label class="control-label " for="gameStatus">Estatus</label>
                    <input class="form-control" id="gameStatus" name="gameStatus" data-validation="alphanum" type="text" placeholder="Estatus" onblur=""/>
                </div>
                <span class="help-block"></span>

                <div class="form-group"> <!-- Subject field -->
                    <label for="gameImg">Select images:</label><br />
                    <input type="file" name="gameImg" multiple>
                </div>
                <span class="help-block"></span>

                <div class="form-group2">
                    <button class="btn btn-primary " name="submit" type="submit" onclick="validateForm();">Submit</button>
                </div>

            </form>

             </div>
            </div>
        </div>
    </div>
</div>
        <%- include footer %>

    <script src="../main.js">
    </script>
  </body>
</html>
